# メニューバーのロゴ表示

- [x] B案の縦タブとウィンドウを単色の輪郭で描画
- [x] 通常表示と保存エラーからの復帰表示を変更
- [x] 明暗・実寸の描画確認と既存テスト・ビルド
- [x] 署名済みの起動用アプリへ反映

背景の角丸タイルを除き、18ptのテンプレート画像として描画する。色はmacOSに任せるため、明るいメニューバーでも暗いメニューバーでも表示できる。保存エラー時の警告アイコンは維持する。

検証：250テスト、本体・PoCのビルド、`git diff --check` が成功。明暗両背景の描画で18ptの視認性を確認した。

反映後はメニューバーに縦タブとウィンドウの輪郭が出ること、クリックで従来どおりメニューが開くことを確認する。

更新した `build/TabDesk.app` の起動を確認済み。旧アプリは `build/backups/TabDesk-before-status-icon-20260905-204614.app` に退避した。
