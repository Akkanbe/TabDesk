# README ホットキー図解の作業記録

- [x] ホットキー既定値、登録先、画面・タブ切替、フォーカス復帰の実装を確認
- [x] タイルの空き選択、分割、割り当て変更と入れ替えの実装を確認
- [x] README に操作一覧と登録・フォーカス移動・タイル割り当ての Mermaid 図を追加
- [x] 差分・図の記述・説明を実装と照合し、既存テストとビルドを実行

## 検証結果

- `./scripts/test.sh`：成功（Core とアプリ層）。
- `swift build -c debug --product TabDesk`：成功。
- `git diff --check`：成功。
- Mermaid 図の実レンダリングと実機での操作確認は未実施。

## 手動確認手順

1. Mermaid 対応の Markdown プレビューで README の「ホットキーで操作する（図解）」を開き、3つの図と表が表示されることを確認する。
2. 同じ画面のタブを左右2タイルに分割して「適用」し、未登録の窓2つを順にフォーカスして `Ctrl+Option+R` で登録する。
3. 2画面環境で `Ctrl+Option+→` / `←`、`Ctrl+Option+1〜9`、`Ctrl+Tab` / `Ctrl+Shift+Tab` を使い、選択画面・タブとフォーカスの復帰を確認する。
4. タイル編集画面で占有済みタイルにもう一方の窓を割り当て、「適用」で2つの窓が入れ替わることを確認する。

実際のウィンドウ操作を伴う上記手順は利用者向け。今回の変更はドキュメントのみ。
